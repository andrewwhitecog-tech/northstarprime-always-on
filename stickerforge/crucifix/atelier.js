(()=>{const cards=[...document.querySelectorAll('.sticker')],previous=document.querySelector('#previous'),next=document.querySelector('#next');if(!cards.length||!previous||!next)return;let active=0;const go=delta=>{active=(active+delta+cards.length)%cards.length;cards[active].scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'});history.replaceState(null,'','#'+cards[active].id)};previous.addEventListener('click',()=>go(-1));next.addEventListener('click',()=>go(1));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)active=cards.indexOf(entry.target)}),{threshold:.55});cards.forEach(card=>observer.observe(card));addEventListener('keydown',event=>{if(['ArrowLeft','ArrowUp','j','J'].includes(event.key))go(-1);if(['ArrowRight','ArrowDown','k','K'].includes(event.key))go(1)})})();
