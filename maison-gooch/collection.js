const pieces=[...document.querySelectorAll('.piece')];const count=document.querySelector('#result-count');document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{const filter=button.dataset.filter;document.querySelectorAll('[data-filter]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));let n=0;pieces.forEach(piece=>{piece.hidden=filter!=='all'&&piece.dataset.collection!==filter;if(!piece.hidden)n++});count.textContent=n+' designs'}));const dialog=document.querySelector('#art-viewer');if(dialog&&typeof dialog.showModal==='function'){document.querySelectorAll('[data-viewer]').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();const img=link.querySelector('img');document.querySelector('#viewer-image').src=link.href;document.querySelector('#viewer-image').alt=img.alt;const title=link.closest('.piece')?.querySelector('h3')?.textContent||'VAPE Aurora Cloud Camo Hoodie';document.querySelector('#viewer-title').textContent=title;document.querySelector('#viewer-caption').textContent='AI design preview · Physical pieces are not yet available to order.';dialog.showModal()}));document.querySelector('#close-viewer').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}})}
